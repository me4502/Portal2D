/*
 * Me4502's Sane Collision Detection v0.1!
 * User Defined Event 0 is collision. 
 *
 * Arguments:
 * (0) - Instance ID
 * (1) - oldX
 * (2) - oldY
 * (3) - newX
 * (4) - newY
 */

//Firstly, let's do a check on the player.
with(argument0) {
    
    event0Cancelled = false;
    
    event_user(0);
    
    if(!event0Cancelled) {
        
    }
}
